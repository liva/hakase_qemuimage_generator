# hakase qemu image generator

## How to use
```
$ ./setup.sh
```
It will generate XXXXXXXXX.tar.xz.

## Warning
Should be run on a fast CPU. (i.e. Intel Xeon Processor)

## Requirements
* wget
* qemu (https://github.com/liva/hakase_qemu_deb)
* cloud-utils
* expect